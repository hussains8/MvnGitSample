 node{
   stage('SCM Checkout'){
    // Clone repo
	git 'https://github.com/hussains8/MvnGitSample'
   
   }
   stage('Compile-Package'){
    // get Maven Home Path
    def mvn = tool (name: 'maven-3', type: 'maven') + '/bin/mvn' 
    sh "${mvn} package"
   
   }
   }
